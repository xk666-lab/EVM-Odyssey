# EIP-3009 转账授权

> 📝 待编写

## 学习目标

- 理解 transferWithAuthorization 机制
- 掌握与 EIP-2612 的区别
- 了解 Coinbase USDC 的实现
- 学会选择合适的授权方案

## 大纲

### 1. EIP-3009 概述

#### 1.1 核心思想
- 用户签名授权转账
- 任何人可以提交签名
- 直接完成转账（不是授权额度）

### 2. 核心接口

```solidity
interface IERC3009 {
    function transferWithAuthorization(
        address from,
        address to,
        uint256 value,
        uint256 validAfter,
        uint256 validBefore,
        bytes32 nonce,
        uint8 v,
        bytes32 r,
        bytes32 s
    ) external;
    
    function receiveWithAuthorization(
        address from,
        address to,
        uint256 value,
        uint256 validAfter,
        uint256 validBefore,
        bytes32 nonce,
        uint8 v,
        bytes32 r,
        bytes32 s
    ) external;
}
```

### 3. 与 EIP-2612 的区别

| 特性 | EIP-2612 (Permit) | EIP-3009 |
|------|-------------------|----------|
| 授权类型 | 授权额度 | 授权转账 |
| 使用次数 | 可多次（直到额度用完） | 一次性 |
| Nonce | 递增 | 随机（bytes32） |
| 灵活性 | 高 | 低 |
| 采用度 | 广泛 | 较少 |

### 4. 使用场景

#### 4.1 Coinbase 的选择
- USDC 使用 EIP-3009
- 更严格的控制
- 每次转账都需要新签名

#### 4.2 适用场景
- 一次性授权转账
- 需要严格控制的场景
- 企业级应用

---

## ✅ 检查清单

- [ ] 理解 EIP-3009 机制
- [ ] 掌握与 EIP-2612 的区别
- [ ] 知道适用场景
- [ ] 会选择合适方案

---

## 📚 参考资料

- [EIP-3009 官方文档](https://eips.ethereum.org/EIPS/eip-3009)
- [USDC 实现](https://github.com/circlefin/stablecoin-evm)

